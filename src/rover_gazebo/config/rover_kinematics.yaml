# Generated by tools/derive_ratios.py -- do not edit by hand.
# Source: /home/pradyun/temp_mrt_ws/reference/urdf_full_withdifferential/urdf/urdf_full_withdifferential.urdf
#
# Every number below is recovered from the CAD export. Re-run the tool after any
# change to the URDF and both the model and the kinematics node pick it up.

rover:
  geometry:
    wheel_radius: 0.149850
    wheel_width: 0.140000
    wheelbase: 0.744729
    track: 0.436000
    ground_below_chassis_origin: 0.722480

  # Wheel stations in base_link axes: x forward, y left, from the rover centre.
  # Joint names carry a _joint suffix; SDF will not load a model whose joint
  # shares a name with a link, and the CAD export gave them matching names.
  # steer_sign is +1 when a positive joint angle points that wheel left.
  # drive_sign is +1 when a positive joint speed rolls the rover forward.
  wheels:
    FL:
      steer_joint: FLE_joint
      drive_joint: FLD_joint
      x: 0.372365
      y: 0.218000
      steer_sign: -1.0
      drive_sign: +1.0
    FR:
      steer_joint: FRE_joint
      drive_joint: FRD_joint
      x: 0.372365
      y: -0.218000
      steer_sign: -1.0
      drive_sign: +1.0
    BL:
      steer_joint: BLE_joint
      drive_joint: BLD_joint
      x: -0.372365
      y: 0.218000
      steer_sign: -1.0
      drive_sign: +1.0
    BR:
      steer_joint: BRE_joint
      drive_joint: BRD_joint
      x: -0.371204
      y: -0.218000
      steer_sign: -1.0
      drive_sign: -1.0

  # Suspension coupling. FLS is the single free coordinate; the rest are
  # torque-driven followers, held by rocker_coupling_node.
  # Positive rocker angle means that wheel moves down relative to the chassis.
  coupling:
    leader: FLS_joint
    followers:
      BLS_joint: -0.577165
      BRS_joint: 0.577165
      FRS_joint: -1.007737
    # Raw pairwise relations, before they are chained onto the leader.
    # rocker_coupling_node needs these, because it applies each constraint
    # as an equal and opposite torque pair on the two joints it ties together.
    four_bar:
      left:  {front: FLS_joint, rear: BLS_joint, ratio: -0.577165}
      right: {front: FRS_joint, rear: BRS_joint, ratio: -0.572734}
    differential:
      joints: [BLS_joint, BRS_joint]
      law: sum_is_zero
    decorative:
      LCON: -1.005672      # of FLS
      RCON: 1.021559      # of FRS
      Differential: 0.194351   # of FLS

  # Linearisation cost of the connector four-bars over +/- 20 deg of front-rocker travel.
  fit_quality:
    left_max_deviation_deg: 0.244
    left_max_wheel_height_error_m: 0.0010
    right_max_deviation_deg: 0.364
    right_max_wheel_height_error_m: 0.0014
