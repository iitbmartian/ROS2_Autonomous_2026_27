slam_toolbox:
  ros__parameters:
    use_sim_time: true 
    # Plugin params
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # ROS Parameters
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    # scan_topic: /scan
    scan_topic: /second_lidar/scan
    use_map_saver: true
    mode: mapping #localization

    # Message filter queue size to prevent dropping messages
    queue_size: 100

    # if you'd like to immediately start continuing a map at a given pose
    # or at the dock, but they are mutually exclusive, if pose is given
    # will use pose
    #map_file_name: test_steve
    #map_start_pose: [0.0, 0.0, 0.0]
    #map_start_at_dock: true

    debug_logging: false
    throttle_scans: 2  # Process every 2nd scan to reduce load
    transform_publish_period: 0.02 #if 0 never publishes odometry
    map_update_interval: 2.0
    resolution: 0.05
    #restamp_tf: true
    restamp_tf: false  # ORIGINAL
    min_laser_range: 0.02 #for rastering images
    max_laser_range: 5.0 #for rastering images
    minimum_time_interval: 0.5
    transform_timeout: 1.0  # Increased from 0.5 
    #tf_buffer_duration: 60.0
    tf_buffer_duration: 30.0  # ORIGINAL
    stack_size_to_use: 40000000 #// program needs a larger stack size to serialize large maps
    enable_interactive_mode: true

    # General Parameters
    use_scan_matching: true
    use_scan_barycenter: true
    #minimum_travel_distance: 0.1  # ALREADY ORIGINAL
    minimum_travel_distance: 0.1   #// meanas the map would update onlywhenthis much moement or this much rotationnwould be done
    minimum_travel_heading: 0.2
    check_min_dist_and_heading_precisely: false
    scan_buffer_size: 20
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1  
    #link_scan_maximum_distance: 3.0
    link_scan_maximum_distance: 1.5  # ORIGINAL
    #loop_search_maximum_distance: 5.0
    loop_search_maximum_distance: 3.0  # ORIGINAL
    do_loop_closing: true
    loop_match_minimum_chain_size: 15            
    #loop_match_maximum_variance_coarse: 3.5 
    loop_match_maximum_variance_coarse: 3.0  # ORIGINAL
    loop_match_minimum_response_coarse: 0.35    
    loop_match_minimum_response_fine: 0.45

    # Correlation Parameters - Correlation Parameters
    
    correlation_search_space_dimension: 0.5  # ORIGINAL
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1 

    # Correlation Parameters - Loop Closure Parameters
    
    loop_search_space_dimension: 8.0  # ORIGINAL
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Scan Matcher Parameters
    distance_variance_penalty: 0.5      
    angle_variance_penalty: 1.0    

    fine_search_angle_offset: 0.00349     
    coarse_search_angle_offset: 0.349   
    coarse_angle_resolution: 0.0349        
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
    min_pass_through: 2
    
    occupancy_threshold: 0.2  # ORIGINAL - probability above which a point is marked occupied